import threading
from connection import *
import threading, time, sys
def ram_timer():
     time.sleep(30)
     #threading.Timer(60.0, ram).start()
  #ram()
  #ram_calcul()
     ram()
     #ram_timer()
     print "done "
